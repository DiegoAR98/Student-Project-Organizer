<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <base href="/">
  <title>SPO</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gradient-to-r from-blue-100 to-blue-300 min-h-screen flex flex-col">
  <header class="bg-gradient-to-r from-blue-800 to-blue-600 text-white p-4 shadow-lg">
    <div class="max-w-6xl mx-auto flex justify-between items-center">
      <h1 class="text-2xl font-bold">
        <a href="/" class="hover:text-blue-200">Student Project Organizer</a>
      </h1>
      <nav class="space-x-4">
        {{#if logged_in}}
          <a href="/" class="hover:text-blue-200">Home</a>
          <a href="/dashboard" class="hover:text-blue-200">Dashboard</a>
          <a id="logout" class="hover:text-blue-200 cursor-pointer">Logout</a>
        {{else}}
          <a href="/login" class="hover:text-blue-200">Login</a>
        {{/if}}
      </nav>
    </div>
  </header>
  <main class="flex-grow container container-fluid mt-5 mx-auto p-4 mt-6 bg-white rounded-lg shadow-md">
    {{{ body }}}
  </main>
  <footer class="p-4 bg-blue-800 text-white text-center text-sm">
    <div class="bg-gradient-to-r from-blue-800 to-blue-600">
      &copy; 2024 Student Project Organizer. All rights reserved.
    </div>
  </footer>
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
